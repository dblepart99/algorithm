#include <bits/stdc++.h>

using namespace std;
using ll = long long;


int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  int t, a, b;
  char c;
  cin >> t;
  while (t--){
    cin >> a >> c >> b;
    cout << a+b << '\n';
  }

  return 0;
}